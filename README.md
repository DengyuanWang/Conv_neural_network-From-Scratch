# Conv_neural_network-From-Scratch
Build a convolutional neural network from scratch, deployed for Handwriting recognition(accuracy=97%), Use adam optimizer

pre-requirments:
opencv
matplotlib
scipy

1. Use "python3 cnn.py -r" to retrain networks
2. Use "Python3 cnn.py" to do prediction using trained networks
